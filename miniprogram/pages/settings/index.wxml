<!--pages/settings/index.wxml-->
<van-cell-group>
    <van-field label="昵称" value="{{ nickName }}" readonly />
    <van-field label="手机号" value="{{ phone }}" placeholder="请输入手机号" />
    <van-field label="地址" value="{{ address }}" placeholder="请输入地址" />
</van-cell-group>
